default_target: qemux86-64
sdk:
  image: ghcr.io/avocado-framework/avocado-sdk:latest
runtime:
  default:
    target: x86_64-unknown-linux-gnu
ext:
  test-both:
    sysext: true
    confext: true
    packages:
    - vim
    files:
      etc/config.conf: key=value
